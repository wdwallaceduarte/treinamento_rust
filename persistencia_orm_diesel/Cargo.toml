[package]
name = "persistencia_orm_diesel"
version = "0.1.0"
edition = "2024"

[dependencies]
diesel = { version = "1.4.1", features = ["mysql"] }
dotenv = "0.15.0"

[dev-dependencies]
diesel_cli = { version = "2.1.1", features = ["mysql"] }
